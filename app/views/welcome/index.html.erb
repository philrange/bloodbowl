


<h2>Hello World</h2>
<p>
  The time is now: <%= Time.now %>
</p>

<p>
<%= link_to 'View All Leagues', leagues_path %>
</p>

<p>
<%= link_to 'Create New League', new_league_path %>
</p>

 <% if user_signed_in? %>

<div>
<h4>My Leagues</h4>
<% leagues = my_leagues(current_user.id) %>
<ul>
<% leagues.each do |league| %>
<li><%= link_to league.name, league_path(league.id) %></li>
<% end %>
</ul>
</div>
 <% end %>
