
$(".item").unbind().bind("ajax:success",function(event,data,status,xhr) {
    var item = $(this).data("item");
    $('.' + item).text("<%= @new_amount %>");
    $('.budget').text("<%= @team.budget %>");
  });
