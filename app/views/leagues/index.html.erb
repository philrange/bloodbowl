<h1>All Leagues</h1>
 
<table>
  <tr>
    <th>League</th>
  </tr>
 
  <% @leagues.each do |league| %>
    <tr>
      <td><%= league.name %></td>
      <td><%= link_to 'Show', league_path(league) %></td>
    </tr>
  <% end %>
</table>